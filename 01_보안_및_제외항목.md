# 보안 및 패키징 안내

이 배포용 압축본은 제품을 다시 실행하고 개발하는 데 필요한 **전체 소스 코드, 정적 자산, 교육과정 데이터, 테스트, 설계·운영 문서, 배포 설정의 구조**를 포함합니다.

## 안전을 위해 치환한 값

`deploy/dev.env`, `deploy/onprem.env`, `deploy/release.env`의 Langfuse 키 값은 다음 형태의 입력용 문구로 치환했습니다.

- `LANGFUSE_SECRET_KEY=replace-with-your-langfuse-secret-key`
- `LANGFUSE_PUBLIC_KEY=replace-with-your-langfuse-public-key`

실행 환경에 맞는 키가 필요할 때만 별도로 입력하세요. 실제 키는 압축본이나 Git 저장소에 커밋하지 마세요.

## 압축본에서 제외한 생성 파일

아래 항목은 소스 코드가 아니므로 제외했습니다.

- `projects/` 아래 로컬 테스트 세션 JSON
- `__pycache__/`, `*.pyc`, `*.pyo`
- `.pytest_cache/`
- `edu_agent.egg-info/`
- 로컬 로그 파일
- Git 내부 메타데이터

이 항목들은 설치·실행·테스트 과정에서 필요에 따라 다시 생성됩니다. 기능 구현 코드와 교육과정 콘텐츠는 제외하지 않았습니다.
