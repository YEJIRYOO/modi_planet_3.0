# MODI Planet 3.0 전체 소스 패키지

이 압축본은 **교육과정(Learn) + AI LAB(Create) + 생성 엔진 + 테스트 + 설계 문서 + 배포 설정**을 모두 포함한 최신 전체 소스입니다.

## 바로 확인할 주소

- 공개 AI LAB: https://modi-planet-3-curriculum.uaiaoapa.chatgpt.site/#create
- 공개 교육과정: https://modi-planet-3-curriculum.uaiaoapa.chatgpt.site/lms
- GitHub 작업 브랜치: https://github.com/uaiaoapa-eng/MODI-Plant3.0/tree/codex/modi-planet-3-curriculum/modi-planet-3-curriculum

## 이번 최신본에 반영된 내용

- 초등·중등·고등 각 9차시, 총 **27개 차시·513개 수업 페이지**
- 차시별 서로 다른 27개 시각 세계관
- 목표·개념·예시·제작·진단·루브릭 등 15종 수업 화면 문법
- Web / MODI 하드웨어 / Web+MODI 프로젝트
- AI LAB 자유 제작 화면과 기존 생성 엔진 연결
- 태블릿·휴대폰에서도 `홈 / 교육과정 / AI LAB` 메뉴명이 항상 표시
- 수업 진행 화면에서도 AI LAB으로 바로 이동하는 버튼 제공
- 공개 사이트에서 `/#create` AI LAB 직접 진입 복구
- 전체 자동 테스트 **962개 통과**

## 폴더별 설명

| 경로 | 내용 |
|---|---|
| `web/` | 홈, AI LAB, 교육과정, 수업 플레이어의 HTML·CSS·JavaScript와 이미지 자산 |
| `curriculum/` | 초등·중등·고등 27개 차시 데이터와 교육과정 로더 |
| `agent/` | 설계 → 구현 → 검증을 처리하는 AI 에이전트 핵심 엔진 |
| `hybrid/` | Web+MODI 하이브리드 실행 관련 코드 |
| `build_template/` | 생성 결과물을 빌드할 때 사용하는 템플릿 |
| `tests/` | API, 교육과정, 모바일 UX, 생성 엔진, RAG 등의 자동 테스트 |
| `docs/` | 아키텍처, API, 운영, RAG, 관측 및 설계 문서 |
| `deploy/` | 운영 배포 관련 설정과 설명 |
| `scripts/` | 배치 작업, 점검, 리포트 및 운영 보조 스크립트 |
| `server.py` | FastAPI 웹 서버와 API 진입점 |
| `main.py` | 콘솔 실행 진입점 |
| `docker-compose.yml` | Docker 기반 전체 실행 구성 |
| `.env.example` | 환경변수 예시. 실제 비밀키는 포함하지 않음 |

## 가장 빠른 로컬 실행 방법

Python 3.11 이상이 필요합니다.

### Windows PowerShell

```powershell
cd MODI-Planet-3.0-Full-Source-and-Docs-2026-08-24
py -3.11 -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -e ".[test]"
Copy-Item .env.example .env
python -m uvicorn server:app --host 127.0.0.1 --port 8877
```

브라우저에서 다음 주소를 엽니다.

- 제품 홈: http://127.0.0.1:8877/
- AI LAB: http://127.0.0.1:8877/#create
- 교육과정: http://127.0.0.1:8877/lms

### macOS / Linux

```bash
cd MODI-Planet-3.0-Full-Source-and-Docs-2026-08-24
python3.11 -m venv .venv
source .venv/bin/activate
python -m pip install -e '.[test]'
cp .env.example .env
python -m uvicorn server:app --host 127.0.0.1 --port 8877
```

## 테스트

```bash
python -m pytest
```

교육과정 UI 관련 핵심 테스트만 실행하려면:

```bash
python -m pytest tests/test_v3_p1.py
```

## 중요한 화면 파일

- AI LAB 마크업: `web/index.html`
- AI LAB 동작: `web/app.js`
- AI LAB 스타일: `web/app.css`
- 교육과정 마크업: `web/lms.html`
- 교육과정 동작: `web/lms.js`
- 교육과정 스타일: `web/lms.css`
- 교육과정 데이터: `curriculum/`

## 설명 문서 읽는 순서

1. `00_먼저_읽어주세요.md` — 압축본 안내와 빠른 실행
2. `01_보안_및_제외항목.md` — 비밀키 치환 및 패키징 제외 항목
3. `README.md` — 전체 기능, API, 실행 및 운영 설명
4. `docs/design/modi-planet-3-architecture.md` — MODI Planet 3.0 제품 아키텍처
5. `docs/modi/modi_playbook.md` — MODI 하드웨어 활용 방식
6. `docs/design/` — RAG, 동시성, 비용, 배포 등 세부 설계

## 환경변수와 비밀키 주의

- 실제 `.env`와 API 키는 이 압축본에 포함하지 않았습니다.
- `.env.example`을 `.env`로 복사한 뒤 필요한 값만 입력하세요.
- API 키, 비밀번호, 토큰이 들어간 `.env`는 GitHub에 올리지 마세요.
- AI 생성 기능은 Claude CLI 로그인 또는 `ANTHROPIC_API_KEY` 설정이 필요합니다.
- 키가 없어도 정적 UI와 교육과정 데이터 구조는 확인할 수 있습니다.

## 제품 구조 요약

```text
사용자
 ├─ 교육과정 Learn
 │   └─ 27개 차시 → 513개 수업 페이지 → 활동·미리보기·MODI
 └─ AI LAB Create
     └─ Web / MODI / Web+MODI 선택
         └─ 설계 → 구현 → 검증 → 결과 미리보기
```

더 자세한 API·RAG·배포·운영 설명은 루트의 `README.md`에 정리되어 있습니다.
